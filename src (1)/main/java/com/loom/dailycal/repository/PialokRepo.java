package com.loom.dailycal.repository;

import com.loom.dailycal.models.Pialok;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PialokRepo extends JpaRepository<Pialok, Integer> {
}
