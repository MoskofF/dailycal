package com.loom.dailycal.repository;

import com.loom.dailycal.models.Cel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CelRepo extends JpaRepository<Cel, Integer> {
}
